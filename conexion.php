<?php
$host = "localhost";
$user = "root";
$pass = "";
$db   = "suva_login";

$conn = new mysqli($host, $user, $pass, $db);

if ($conn->connect_error) {
    die("Error de conexión a la base de datos");
}
?>
<?php
$host = "localhost";
$user = "root";
$pass = "";
$db   = "suva_login";

$conn = new mysqli($host, $user, $pass, $db);

if ($conn->connect_error) {
    die("Error de conexión a la base de datos");
}
?>
<?php